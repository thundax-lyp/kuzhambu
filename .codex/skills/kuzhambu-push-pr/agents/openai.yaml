interface:
  display_name: "Kuzhambu Push PR"
  short_description: "Push branch work and create or update a PR."
  default_prompt: "Use $kuzhambu-push-pr to push this branch and create or update the PR."

policy:
  allow_implicit_invocation: false
