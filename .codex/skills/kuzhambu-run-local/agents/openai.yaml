interface:
  display_name: "Run Local"
  short_description: "Start selected Kuzhambu local test targets."
  default_prompt: "Use $kuzhambu-run-local with admin portal to start the local test environment."

policy:
  allow_implicit_invocation: false
