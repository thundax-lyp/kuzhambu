# AI Config Prompt Requirements

## Purpose

AI 配置与提示词模块定义 AI 服务、模型能力和提示词模板的后台管理需求。

## Scope

覆盖：
- 主 AI 服务和备用 AI 服务配置。
- 模型列表管理。
- 能力到模型的映射。
- 模型连通性测试。
- AI 调用延迟、失败和成本统计。
- 提示词编辑、变量校验、版本历史和回滚。
- AI 优化提示词建议。
- AI 能力可用性状态检查。
- AI 功能动作状态面板。

不覆盖：
- 具体业务 AI 功能的候选结果确认。
- 搜索、问答或图谱的业务编排。
- 具体 AI 生成结果的人工精修。


## Functional Requirements

- 必须支持主 AI 服务和备用 AI 服务分别配置。
- 必须支持主服务不可用时切换至备用服务，并在管理后台展示降级状态。
- 必须支持主服务恢复后的状态展示。
- 必须支持主服务和备用服务分别维护模型列表。
- 必须支持模型新增、编辑、启用、禁用、删除和连通性测试。
- 必须支持记录和查看模型检测历史。
- 必须支持为 AI 能力选择模型。
- 模型选择候选必须来自已登记且已启用的模型，不要求管理员手动输入模型名。
- 必须支持模型能力校验，避免将不具备相应能力的模型绑定到业务能力。
- 必须支持完整 AI 能力范围的模型映射：翻译、标签、视觉描述、信息融合、问答、条目拆分、图片理解、图片生成、知识图谱、摘要、版本摘要。
- 必须支持提示词按知识库范围和 AI 能力组合进行管理。
- 必须支持提示词编辑、版本、版本对比、回滚和变量校验。
- 必须实时解析提示词变量并展示变量列表。
- 必须支持 AI 优化提示词建议，建议需确认后才可应用。
- 必须在提示词保存或回滚后检查相关 AI 功能动作是否仍可用。
- 必须提供 AI 功能动作状态面板，展示各知识库 AI 动作是否可用及不可用原因。
- 必须展示 AI 调用延迟、失败和成本统计。

## Business Rules

- AI Key 不得暴露给前端。
- 模型能力与业务能力不匹配时不得保存映射。
- 删除模型时，若该模型仍被 AI 能力映射使用，必须阻止删除并提示先修改映射。
- 提示词缺失时 AI 业务功能必须失败并提示配置异常。
- 不得使用隐藏兜底提示词。
- 提示词变量必须使用统一占位符格式，并在保存和调用前校验。
- 同一知识库范围和 AI 能力组合必须只有一个当前生效提示词。
- 提示词更新后对新调用生效，已在执行中的调用不受影响。
- 配置变更只影响新的调用，不应破坏已保存内容、已上传文件或已生成产物。
- AI 语义层失败不得自动重试，应提示用户手动重试。
- 网络传输层失败允许触发主备切换。

## Acceptance Criteria

- 管理员能测试模型连通性并看到结果。
- 管理员能查看模型检测历史。
- 管理员无法删除仍被 AI 能力映射使用的模型。
- 管理员为某项 AI 能力选择能力不匹配的模型时保存失败并看到原因。
- 提示词变量缺失时保存或调用失败并给出明确提示。
- 管理员能对比两个提示词版本。
- 管理员能回滚提示词历史版本。
- 提示词保存或回滚后，管理员能看到受影响 AI 功能动作是否可用。
- AI 服务异常时管理员能看到降级或失败状态。
- 主服务不可用且备用服务可用时，业务 AI 功能不中断，管理后台显示降级状态。

## Related Documents

- [AI-REFINEMENT-REQUIREMENTS.md](./AI-REFINEMENT-REQUIREMENTS.md)：消费模型和提示词配置完成翻译、标签、摘要、问答对、图片理解和条目拆分。
- [SANCAI-KNOWLEDGE-REQUIREMENTS.md](./SANCAI-KNOWLEDGE-REQUIREMENTS.md)：消费模型和提示词配置完成视觉描述、信息融合和图片生成。
- [KNOWLEDGE-GRAPH-REQUIREMENTS.md](./KNOWLEDGE-GRAPH-REQUIREMENTS.md)：消费模型和提示词配置完成实体关系提取。
- [QA-REQUIREMENTS.md](./QA-REQUIREMENTS.md)：消费模型和提示词配置完成回答生成。
- [OPERATIONS-REQUIREMENTS.md](./OPERATIONS-REQUIREMENTS.md)：查看 AI 调用统计、健康状态和异常日志；不管理 AI 配置本身。

## Open Items

无
